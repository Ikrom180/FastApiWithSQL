python -m venv .venv
source .venv/bin/activate
python -m pip install flask
python -m pip install flask-sqlalchemy
python -m pip install fastapi pydantic uvicorn
pip install fastapi uvicorn
uvicorn foldername:app --reload -> to run the server

db.query(User).all()      # Read
db.add(new_user)          # Write
db.commit()               # Save changes
db.delete(user)           # DELETE
db.close()                # Return the car (close connection)

# Query , Path , Header

python -m uvicorn myapi2:app --reload
